import React from 'react';
import './explicit.sass';

export const Explicit: React.FC = () => (
  <div className="explicit">
    <span className="explicit__value">
      Explicit
    </span>
  </div>
);